package com.pdgc.general.structures.container;

import com.pdgc.general.structures.RightType;

/**
 * Interface whose existence is to define that the composite structure has a
 * right type on it
 * 
 * @author Vishal Raut
 */
public interface IRightTypeContainer {
	RightType getRightType();
}
