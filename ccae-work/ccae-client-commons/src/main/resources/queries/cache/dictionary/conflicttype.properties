FETCH_ALL_QUERY_CONFLICT_TYPE=\
    SELECT \
        id, \
        description, \
        sourceTypeId \
    FROM masterData.conflictType

LOOK_UP_QUERY_CONFLICT_TYPE=\
    SELECT \
        id, \
        description, \
        sourceTypeId \
    FROM masterData.conflictType \
    WHERE id = :conflictTypeId