FETCH_ALL_QUERY_CUSTOMER_GROUP=\
    SELECT \
        id, \
        name \
    FROM masterData.customerGroup
    
LOOK_UP_QUERY_CUSTOMER_GROUP=\
    SELECT \
        id, \
        name \
    FROM masterData.customerGroup \
    WHERE id = :customerGroupId