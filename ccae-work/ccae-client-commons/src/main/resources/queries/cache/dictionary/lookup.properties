FETCH_ALL_QUERY_LOOKUP=\
    SELECT \
        id, \
        lookupTypeId, \
        name, \
        code \
    FROM masterdata.lookup

LOOK_UP_QUERY_LOOKUP=\
    SELECT \
        id, \
        lookupTypeId, \
        name, \
        code \
    FROM masterdata.lookup \
    WHERE id = :lookupId