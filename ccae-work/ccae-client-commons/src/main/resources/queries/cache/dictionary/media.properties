FETCH_ALL_QUERY_MEDIA=\
    SELECT \
        id, \
        name, \
        mediacode as shortName \
    FROM masterData.media

LOOK_UP_QUERY_MEDIA=\
    SELECT \
        id, \
        name, \
        mediacode as shortName \
    FROM masterData.media \
    WHERE id = :mediaId

    