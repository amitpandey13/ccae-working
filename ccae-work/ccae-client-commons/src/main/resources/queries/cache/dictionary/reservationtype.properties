FETCH_ALL_QUERY_RESERVATION_TYPE=\
    SELECT \
        id, \
        name \
    FROM masterdata.reservation_type_lookup

LOOK_UP_QUERY_RESERVATION_TYPE=\
    SELECT \
        id, \
        name \
    FROM masterdata.reservation_type_lookup \
    WHERE id = :reservationTypeId