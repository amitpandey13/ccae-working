FETCH_ALL_QUERY_CUSTOMER_TYPE=\
    SELECT \
        id, \
        name \
    FROM masterData.customer_type
    
LOOK_UP_QUERY_CUSTOMER_TYPE=\
    SELECT \
        id, \
        name \
    FROM masterData.customer_type \
    WHERE id = :id::text --why is this a varchar?