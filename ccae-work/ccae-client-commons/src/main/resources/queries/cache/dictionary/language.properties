FETCH_ALL_QUERY_LANGUAGE=\
    SELECT \
        id, \
        name, \
        languageCode \
    FROM masterData.language

LOOK_UP_QUERY_LANGUAGE=\
    SELECT \
        id, \
        name, \
        languageCode \
    FROM masterData.language \
    WHERE id = :languageId
