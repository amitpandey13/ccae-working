FETCH_ALL_QUERY_CUSTOMER_GENRE=\
    SELECT \
        id, \
        name \
    FROM masterData.customer_genre
    
LOOK_UP_QUERY_CUSTOMER_GENRE=\
    SELECT \
        id, \
        name \
    FROM masterData.customer_genre \
    WHERE id = :id::text --why is this id a varchar?